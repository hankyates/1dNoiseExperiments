{
  "name": "wat",
  "version": "1.0.0",
  "description": "",
  "main": "noise.js",
  "scripts": {
    "clean": "rm -rf build/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/app.js > build/bundle.js",
    "watch": "watch 'npm run build' src/",
    "livereload": "livereload build/",
    "serve": "python -m SimpleHTTPServer"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^12.0.1",
    "lodash": "^3.10.1",
    "watch": "^0.16.0"
  }
}
